# CAS-FEE_Project2 - Frontend

## Bootstrap Application

1. `npm install`
2. `npm run typings install`
3. run gulp default

## Install WebDriver for Protractor
node node_modules/protractor/bin/webdriver-manager update