import {Route} from '@angular/router';
import {ShoppingListComponent} from './shopping-list.component';

export const shoppingListRoute: Route = {
    path: 'shopping-list',
    component: ShoppingListComponent
};